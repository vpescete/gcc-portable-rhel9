#include <stdio.h>
#include <math.h>

int main() {
    double x = 4.0;
    printf("sqrt(%.2f) = %.2f\n", x, sqrt(x));
    printf("sin(PI/2) = %.2f\n", sin(M_PI/2));
    printf("cos(0) = %.2f\n", cos(0));
    return 0;
}
